# Plan — Agent: tx-decoder (Wave A)

**Profile:** `tx-decoder`  
**Workspace:** `dir:/home/asyam/dev/Project/aegis-hackathon-kit/aegis-rpc`

## Goal

Normalize unsigned / raw transaction payloads into `TxIntent`; decode ERC20 `approve` / `transfer` (and selectors needed for MVP) per spec; feed **policy** without emitting verdicts.

## Read order

- `../docs/` API + preflight shapes (04–06 as applicable)
- `.hermes/plans/plan-chunks/09-9-transaction-decoder-design.md`

## OpenSrc gate

Run **tx-decoder** block in `../docs/research/agent-research-assignments.md` (viem decode, OZ IERC20, Rabby/Tenderly decode patterns).

## Proposed approach

- `viem` `decodeFunctionData` / `parseAbi` for known ABIs
- Explicit handling for `approve(spender, amount)` with `MaxUint256` detection
- Unknown selector → safe classification path for demo (document fallback)

## Files likely to change

- Decoder module under `packages/` or `apps/web` per repo layout
- Types shared with preflight route

## Tests / validation

- Unit tests: fixtures for approve unlimited, benign transfer, unknown selector
- Integration: preflight request with sample calldata from `curl-demo` or fixtures

## Definition of done

- Decoder never calls AI
- Outputs consumed by policy + audit context

## Risks

- Over-scoping universal router — stay MVP per `ORCHESTRATOR.md` hard limits
