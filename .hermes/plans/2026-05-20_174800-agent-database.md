# Plan — Agent: database (cross-wave; early with preflight)

**Profile:** `database`  
**Workspace:** `dir:/home/asyam/dev/Project/aegis-hackathon-kit/aegis-rpc`

## Goal

**Every decision → audit event** persisted with stable schema; repositories for insert/list; seed data if needed for dashboard demo.

## Read order

- `.hermes/plans/plan-chunks/13-13-database-audit-log-design.md`
- Kit docs touching audit / events in `../docs/`

## OpenSrc gate

Run **database** block in `../docs/research/agent-research-assignments.md` (Supabase client patterns, Next server data access).

## Proposed approach

- Define event row: verdict, reasonCode (nullable unless BLOCK), adapter signals snapshot, memo ref, timestamps
- API used by dashboard poll route and `/demo/agent` — align field names with `../docs/04-api-spec.md`
- If using SQLite/Prisma vs Supabase, match existing repo choice — extend, don’t rewrite

## Files likely to change

- Schema/migrations, `lib/db`, repository modules
- API routes that write audit after preflight

## Tests / validation

- Integration: preflight creates row; list endpoint returns it
- `npm run build` if types surface to web

## Definition of done

- No audit skip on any verdict path
- **QA** can assert row count or IDs in `curl-demo.sh`

## Risks

- Migrations on deployed env — out of scope unless orchestrator approves ops change
