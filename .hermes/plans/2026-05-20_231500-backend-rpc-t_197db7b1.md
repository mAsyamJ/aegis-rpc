# Sub-plan — backend-rpc §8 API gateway (Kanban t_197db7b1)

**Time:** 2026-05-20T23:15:00Z  
**Workspace:** dir:/home/asyam/dev/Project/aegis-hackathon-kit/aegis-rpc  
**Parent:** t_e69aadd4  
**Refs:** kit docs/04-api-spec.md, .hermes/plans/2026-05-20_174800-agent-backend-rpc.md

## Plan

Verify POST /api/rpc passthrough + intercept matches spec; unblock `npm run build`; extend curl smoke for eth_sendTransaction.

## Analyze

- `lib/rpc/client.ts` already lists passthrough + intercepted per 04-api-spec (incl. eth_sendTransaction, eth_getBlock*, eth_getLogs).
- `api/rpc/route.ts` uses zod, `buildInterceptScreeningError`, no policy in route.
- Build failed: missing `@tanstack/react-query`; unused `lib/db/sqliteClient.ts` imported missing `seedAuditEvents` and better-sqlite3 (nothing imported sqliteClient — removed dead module).

## Build

- apps/web: add `@tanstack/react-query`; remove `src/lib/db/sqliteClient.ts`.
- tests/curl-demo.sh: assert eth_sendTransaction returns -32090 + REQUIRES_PREFLIGHT.
- plan-chunks/08: intercepted line includes eth_sendTransaction (align with kit spec).

## Test

- `cd contracts && forge test`
- `npm run build --prefix apps/web`
- Dev server + `AEGIS_BASE_URL=http://127.0.0.1:3000 ./tests/curl-demo.sh`

## Integrate

- Kanban comment on parent t_e69aadd4 (plan path + status).

---

## Verification (2026-05-20 run, Kanban t_197db7b1)

- Spec cross-check: `buildInterceptScreeningError` in `apps/web/src/lib/rpc/client.ts` matches kit `docs/04-api-spec.md` (-32090, message, `data.verdict` / `reasonCode` / `broadcasted` / `method`).
- Passthrough set includes all methods listed in 04-api-spec for read/gas helpers.
- Intercepted set includes `eth_sendRawTransaction`, `eth_sendTransaction`, `aegis_preflight`, `aegis_sendTransaction`.
- `forge test` (contracts): 5 passed.
- `npm run build --prefix apps/web`: success (Next 16.2.6).
- `AEGIS_BASE_URL=http://127.0.0.1:3000 ./tests/curl-demo.sh`: OK (includes 2a `eth_sendTransaction` intercept).
- App repo HEAD: `e4098ec5b7b88246e3fb1f48714236b1b0f71df4`.
