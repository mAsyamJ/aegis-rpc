# Plan — Agent: ai-memo (Wave B)

**Profile:** `ai-memo`  
**Workspace:** `dir:/home/asyam/dev/Project/aegis-hackathon-kit/aegis-rpc`

## Goal

Post-verdict **explanation only**: human-readable memo from deterministic context (verdict, signals, decoded intent). **Never** override SAFE/WARN/BLOCK; template fallback when model unavailable.

## Read order

- `.hermes/plans/plan-chunks/14-14-ai-transaction-analysis-layer.md`
- `AGENTS.md` non-negotiables (AI explains, policy decides)

## OpenSrc gate

Run **ai-memo** block in `../docs/research/agent-research-assignments.md` (Rabby/wallet-guard explanation copy patterns).

## Proposed approach

- Input: frozen JSON context from preflight pipeline (no raw keys)
- Output: memo string + optional structured bullets for UI
- Ordering enforced in code: **policy verdict computed → audit row planned → memo generated → response assembled**

## Files likely to change

- Memo service module, API response field on `/api/preflight` or equivalent
- Prompt templates (versioned in repo, no secrets)

## Tests / validation

- Unit: deterministic template path when AI disabled
- Contract test: memo field absent or static in fixture mode does not change verdict

## Definition of done

- Demo shows memo alongside verdict in dashboard or `/demo/agent`
- `docs/AI_COLLABORATION_LOG.md` row if AI-assisted prompt work (per team rule)

## Risks

- Latency — keep memo optional/async for MVP if spec allows; document behavior
