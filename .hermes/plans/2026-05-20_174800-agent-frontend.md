# Plan — Agent: frontend (Wave C)

**Profile:** `frontend`  
**Workspace:** `dir:/home/asyam/dev/Project/aegis-hackathon-kit/aegis-rpc`

## Goal

Professional **OpsRisk** dashboard: timeline, verdict badges, adapter signals, AI memo panel; **LEAD demo** `/demo/agent` per `docs/15-demo-script.md`; UX consistent with serious security product (not generic AI slop).

## Detailed UI plan (supersedes / extends this file)

Implement layout + Reown + TanStack Query per:

- `.hermes/plans/2026-05-20_202600-frontend-landing-reown-dashboard.md` (landing vs `(app)` dashboard shell, polling, Reown AppKit + wagmi, Base Sepolia)

## Read order

- `.hermes/plans/plan-chunks/07-7-frontend-technical-plan.md`
- Kit `../docs/` dashboard + demo sections (through 15)
- `../docs/research/agent-research-assignments.md` § frontend

## OpenSrc gate

Run **frontend** block in `../docs/research/agent-research-assignments.md` (shadcn registry, TanStack Query, Rabby/Tenderly UX patterns).

## Proposed approach (summary)

- **Route groups:** `/` = marketing landing **without** `AppShell`; dashboard routes under `(app)` with sidebar
- **Data:** TanStack `useQuery` + `refetchInterval` for audit timeline
- **Wallet:** Reown AppKit + wagmi; connect on `/demo/wallet` and any flow needing wallet
- **Nav:** Rename `/` nav label from “Overview” to “Home” or remove from shell nav entirely

## Files likely to change

- `apps/web/src/app/**` layouts, `components/layout/*`, demo pages
- `package.json` deps for `@tanstack/react-query`, `@reown/appkit*` / wagmi (exact packages per chosen stack)

## Tests / validation

- `npm run build --prefix apps/web`
- Manual: `/demo/agent` scenarios with dashboard visible

## Definition of done

- Demo gate UI checklist satisfied with live API
- No secrets in `NEXT_PUBLIC_*` beyond safe chain IDs / project IDs (WalletConnect project id is public but treat as config)

## Risks

- WSL / Windows browser: `127.0.0.1` vs portproxy — document for pitch/demo
