# Plan — Agent: qa (Wave D + continuous)

**Profile:** `qa`  
**Workspace:** `dir:/home/asyam/dev/Project/aegis-hackathon-kit/aegis-rpc`

## Goal

Own **acceptance automation**: `tests/curl-demo.sh`, `forge test`, `npm run build --prefix apps/web`, and scripted checks for `/demo/agent` scenarios; block release until demo gate green or issue filed with owner.

## Read order

- `.hermes/plans/plan-chunks/20-20-testing-plan.md`
- `../docs/15-demo-script.md`
- `HACKATHON.md` checklist if present

## OpenSrc gate

Run **qa** block in `../docs/research/agent-research-assignments.md` (execution-apis error codes, viem test utils, wallet-guard test ideas).

## Proposed approach

- Extend `curl-demo.sh` for each API milestone (rpc, preflight, audit list)
- Add minimal CI script or document one command for orchestrator to run each wave
- After **approved** on-chain deploy: add assertions for `onChainPolicyHash` / registry address consistency (non-secret values only)

## Files likely to change

- `tests/curl-demo.sh`, `package.json` scripts, GitHub workflow if exists under `aegis-rpc`

## Tests / validation

- All above green on clean checkout with documented env vars (`AEGIS_BASE_URL`, RPC URL, no private keys in files)

## Definition of done

- Demo gate commands reproducible in README snippet (coordinate with **pitch**)
- Each wave closes with QA sign-off comment for orchestrator

## Risks

- Flaky live feed — use fixture mode or documented `STALE_FEED` path from demo script
