# Plan — Agent: adapter (Wave A)

**Profile:** `adapter`  
**Workspace:** `dir:/home/asyam/dev/Project/aegis-hackathon-kit/aegis-rpc`

## Goal

**Chainlink as one adapter:** `ChainlinkPriceAdapter` using `AggregatorV3Interface` — `latestRoundData`, `decimals`, freshness / heartbeat, USD signal shape per `AdapterSignal` contract in `AGENTS.md`.

## Read order

- `.hermes/plans/plan-chunks/11-11-adapter-layer-design.md`
- Kit adapter / API docs in `../docs/`

## OpenSrc gate

Run **adapter** block in `../docs/research/agent-research-assignments.md` (Chainlink AggregatorV3, viem `readContract`, wallet-guard simulation patterns).

## Proposed approach

- Implement adapter interface returning structured **OK/WARN/BLOCK** (or mapped) signal — align names with existing code
- Stale feed → WARN or BLOCK per `DECISIONS.md` / spec; **must not** deadlock demo — document fixture fallback for local demo if feed fails (orchestrator visibility)

## Files likely to change

- `packages/*adapter*` or `src/adapters/`
- Config: Base Sepolia feed address via env (no secrets in repo)

## Tests / validation

- Unit tests with mocked `readContract` responses (stale vs fresh)
- Live read optional behind env flag

## Definition of done

- Adapter does not enforce final verdict alone — **policy** merges signals
- Signal visible in OpsRisk UI when wired

## Risks

- Wrong feed address — pair with **smart-contract** / deploy task for canonical config
